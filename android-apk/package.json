{
  "name": "boulangerie-lomoto-android",
  "version": "1.3.21",
  "private": true,
  "scripts": {
    "build:web": "cd ../web-mobile-app && npm install && npm run build",
    "add:android": "npx cap add android",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "prepare": "npm run build:web && npm install && npm run sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/cli": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  }
}
